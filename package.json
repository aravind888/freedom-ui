{
  "devDependencies": {
    "node-sass": "^8.0.0",
    "nodemon": "^2.0.22"
  },
  "scripts": {
    "css": "node-sass --include-path scss scss/style.scss style.css",
    "watch": "nodemon -e scss -x 'npm run css'"
  }
}
